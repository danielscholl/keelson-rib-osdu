{
  "name": "@keelson/rib-osdu",
  "version": "0.5.0",
  "description": "OSDU CIMPL bridge as a Keelson rib — live cluster/Flux topology and (later) platform-health lanes.",
  "license": "Apache-2.0",
  "author": "Daniel Scholl",
  "type": "module",
  "main": "./src/index.ts",
  "files": [
    "src",
    "bin",
    "workflows",
    "README.md",
    "LICENSE",
    "NOTICE"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "collect:cluster": "bun bin/collect-cluster.ts",
    "collect:topology": "bun bin/collect-topology.ts",
    "collect:quality": "bun bin/collect-quality.ts",
    "collect:features": "bun bin/collect-features.ts",
    "collect:security": "bun bin/collect-security.ts",
    "collect:waiting": "bun bin/collect-waiting.ts",
    "link:keelson": "bun dev/link.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.2",
    "@types/bun": "latest",
    "typescript": "^6.0.3"
  },
  "peerDependencies": {
    "@keelson/shared": ">=0.51.0"
  },
  "peerDependenciesMeta": {
    "@keelson/shared": {
      "optional": true
    }
  }
}
